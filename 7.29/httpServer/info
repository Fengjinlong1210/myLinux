使用封装好的tcp server类, 创建一个http server
当服务器运行起来后, 会不断的接受客户端发来的请求信息
每收到一个信息后, 创建一个线程执行处理任务

处理任务: 对客户端发来的请求进行读取
然后进行反序列化, 提取出客户端要访问的方法、路径、协议版本、文件后缀
再根据这些信息读取服务器目录的文件内容
读取文件时, 要先计算出要读取的文件大小, 然后将文件读到字符串中(网络文件可能包含\0, 所以要完整的读取)

将文件内容作为有效载荷, 再添加上响应的信息, 例如版本、状态码、长度、类型

